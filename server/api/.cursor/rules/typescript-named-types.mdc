---
description: Always use named types instead of inline object types
globs: "**/*.ts"
alwaysApply: false
---

# Named Types Over Inline Object Types

Always define a named `interface` or `type` for object shapes, even when used only locally in one file. Never use inline `{ key: type }` as a return type or parameter type.

```typescript
// BAD
async process(): Promise<{ count: number; failed: number }> { ... }

// GOOD
interface ProcessResult {
   count: number;
   failed: number;
}
async process(): Promise<ProcessResult> { ... }
```

This also forbids inline **intersections** at call sites (e.g. `SomeContract & { newMethod?: ... }`). Adding optional methods to a contract at the call site bypasses `implements`-clause checking — see `interface-contract-integrity.mdc`.
